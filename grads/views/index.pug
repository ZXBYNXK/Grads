//- Index.pug (Landing page)


//- About pug
//- In pug you only need to write tags once
//- Example:
//-     h1 hello world

//- If you want to create nested elements then you use, spaces or tabs.
//- Example:
//-     div
//-         h1 hello world

//- If you want to set attributes in for the tags then use perenthesis
//- Example:
//-     div(id="id_name", class="class_name")
//-         h1 hello world


//- #1 (Initial setup of any html document)
//- doctype html tag
//- This will help identify the page as html just in case it needs to. 
//- The following renders to <!DOCTYPE html>
doctype html 


//- #2 (Initial setup of any html document)
//- html tag
//- Contents:
//-     This tag holds the whole html document
//-     The following renders to <html>...</html> 
html
   
    //- #3 
    //- (Initial setup of any html document)
    //- Add a head tag
    //- This tag holds your links of any type 
    //- Contents: 
    //-     Commonly you will see links to CSS files, 
    //-     JavaScript files, or third party services (Bootstrap, JQuery, Axios, etc)
    //-     The following renders to <head>...</head> -- Last traslation Should get the point. 
    head 
        title 🎓Grads
        meta(name="viewport" content="width=device-width, initial-scale=1.0")        
        //- #5 
        //- Linking the javascript and stylesheet from the public static folder
        link(rel="stylesheet", type="text/css", href="../styles.css")
        script(src="../../index.js" defer)
        
    //- #4 
    //- (Initial setup of any html document)
    //- Add a body tag
    //- Contents:
    //-     This will contain all of the elements 
    //-     that display & interact with the client.      
    body
        //- #6
        //- Creating the banner for homepage         
        //- header (Used for top page elements)
        //- Contents:
        //-     Notice there are IDs and Classes These are available
        //-     for css or javascript files to access as long as the css & js file(s)
        //-     are imported like above in #5. Javascript and CSS both can change the
        //-     the style of the DOM (Document Object Model) but that is the only thing CSS can do.
        //-     Javascript is able to access and modify all the contents of the DOM.
        header(class="container_row_1" id="header") 
            span(id="heading_text") 🎓 Grads
        
        //- #7 
        //- Creating the menu
        //- Div (Commonly used to contain child elements)
        //- Buttons 
        //- Contents: 
        //-     These buttons will toggle other div elements
        //- to either appear or dissapear from the screen.
        //- By default none of the following will show up.
        div(class="container_row_1")
            button(class="header_buttons" id="signup_button") Sign Up
            button(class="header_buttons" id="search_button") Search
            button(class="header_buttons" id="headline_button") Headline
        br
        
        
        div(id="headline")
            pre(style="padding:20px"). 
                Welecome to the Graduate Project
                Sign up to get started!
        br             
        
        
        form(id="form_box1" name="grad_form")
            
            h1(style="maegin-left: 1px;") 🎓 New Graduate ?
            
            label(for="firstname") Firstname            
            input(name="firstname" type="text" placeholder="John" required)
            
            label(for="lastname") Lastname            
            input(name="lastname" type="text" placeholder="Doe" required)            
            

            label(for="email") Email
            input(name="email" type="email" placeholder="name@email.com" required)            
            

            label(for="jobtitle") JobTitle            
            input(name="jobtitle" id="job_title" placeholder="Optional" type="text")    
            
            span(id="extend_div")
                label(for="imageurl") Image url 
                br
                input(name="avatar" type="text")
            
            span(id="extend2_div")
                label(for="about") About 
                br
                textarea(name="about" placeholder="optional")                
            
            span(id="extend3_div") 
                label(for="education") Education
                br
                textarea(name="education" type="textarea")

            button(type="submit" class="header_buttons" id="signup_submit") Sign up
        

        h1(align="center") Current Graduates 
        
        div(id="search_div")        
            input(type="text" id="search_input" placeholder="Search Graduates")                      
        
        div(class="container_row_1" id="show_graduates") 
    

    footer(id="footer1") 
        span(style="font-size: 30px;") 🎓Grads 
        br
        span(style="font-size: 10px") &copy; 2020 By Darius Rain 
